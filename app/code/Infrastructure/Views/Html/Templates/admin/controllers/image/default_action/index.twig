{#
    object          data            Romchik38\Site2\Infrastructure\Controllers\Actions\GET\Admin\Image\DefaultAction\ViewDto
#}

<div class="container-md my-3">
    <div class="row">
        <h1 class="text-center">{{ data.name }}</h1>
        <p>{{ data.description }}</p>

        {% if data.imagesList|length == 0 %} 
            {% include controller_template_dir ~ '/zero_images.twig' with {'t': translate} only %}
        {% else %}
            {% include controller_template_dir ~ '/images.twig' with {'imagesList': data.imagesList, 't': translate, 'u': u, 'paginationHtml': data.paginationHtml, 'paginationForm': data.paginationForm } only %}
        {% endif %}

    </div>
</div>