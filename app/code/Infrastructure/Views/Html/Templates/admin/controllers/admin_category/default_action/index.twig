{#
    string          urlNew                  url to create new category
#}

{%  set  urlNew = u.fromArray(['root', 'admin', 'category', 'new']) %}

<div class="container-md my-3">
    <div class="row">
        <h1 class="text-center">{{ data.name }}</h1>
        <p>{{ data.description }}</p>

        {% if data.categoryList|length == 0 %} 
            {% include controller_template_dir ~ '/zero_categories.twig' with {'t': translate, 'urlNew': urlNew} only %}
        {% else %}
            {% include controller_template_dir ~ '/categories.twig' with {'data': data, 't': translate, 'u': u, 'urlNew': urlNew } only %}
        {% endif %}

    </div>
</div>