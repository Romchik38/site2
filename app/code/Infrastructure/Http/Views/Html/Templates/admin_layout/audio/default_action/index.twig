{#
    object          data            Romchik38\Site2\Infrastructure\Http\Actions\GET\Admin\Audio\DefaultAction\ViewDto
#}

{% extends "base_admin_layouts.twig" %}

{% block content %}
{% set newUrl = u.fromArray(['root', 'admin', 'audio', 'new' ]) %}

<div class="container-md my-3">
    <div class="row">
        <h1 class="text-center">{{ data.name }}</h1>
        <p>{{ data.description }}</p>

        {% if data.audioList|length == 0 %} 
            {% include template_full_prefix ~ '/zero_audio.twig' with {'t': translate, 'newUrl': newUrl} only %}
        {% else %}
            {% include template_full_prefix ~ '/audio.twig' with {'data': data, 't': translate, 'u': u, 'newUrl': newUrl } only %}
        {% endif %}

    </div>
</div>
{% endblock %}